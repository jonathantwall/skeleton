package com.example;

public class ProjectRunner {
  public static void main(String args[]) {
    Greeting.sayHi();
      // TODO - make sure we are running the component compiler and uncomment.
      // ToyApp toyApp = DaggerRunner_ToyComponent.builder().build().toyApp();
  }
}
